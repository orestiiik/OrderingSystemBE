type Ingredient {
    id: String,
    name: String,
    canBeExtra: Boolean,
    extraPrice: Float
}

input IngredientInput {
    name: String,
    canBeExtra: Boolean,
    extraPrice: Float
}

input UpdateIngredientInput {
    id: String,
    name: String,
    canBeExtra: Boolean,
    extraPrice: Float
}

extend type ItemData {
    ingredients: [Ingredient]
}
